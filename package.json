{
  "name": "blind-market",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check:circuits": "nargo compile --program-dir circuits/commit && nargo compile --program-dir circuits/claim",
    "build:contract": "cargo build --manifest-path contracts/blind_market/Cargo.toml --target wasm32v1-none --release"
  },
  "dependencies": {
    "@aztec/bb.js": "^0.87.0",
    "@aztec/foundation": "^0.87.0",
    "@noir-lang/noir_js": "^1.0.0-beta.9",
    "@stellar/stellar-sdk": "^16.0.1",
    "dotenv": "^16.4.7"
  }
}
